main() {
    var option;
    do{
        prints("hello, world\n");
        option = reads();
        if (size(option) == 0) {
            option = 'N';
        } else {
            option = get(option, 0);
        }
    }while(option == 'y');
    return 42;
}